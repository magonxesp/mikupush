import 'package:flutter/material.dart';

class UploadingListTab extends StatelessWidget {
  const UploadingListTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Uploading list'),);
  }
}